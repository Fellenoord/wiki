By default Node.js comes with a package manager called Node Package Manager (NPM). However, we use the Yarn package manager. To install:

```bash
npm install --global yarn
```

Check your installation using `yarn --version`.